# Shared User Namespace

This folder holds shared templates and metadata intended for use across users and
projects.

Version in Git:
- `mouse_info/`
- `session_templates/`
- `presets/`

Do not version in Git:
- Per-session generated files
- Runtime outputs
- Daily mutable run artifacts
